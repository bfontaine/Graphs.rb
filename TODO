- move GDF::Graph to a new Graph module (graph.rb), and use it in gdf.rb instead
  of GDF::Graph. GDF and GEXF modules should extend Graph methods (for example,
  adding #to_gdf or #to_gexf)
- Add Graph#|(*others) and replace Graph#&(other) with Graph#&(*others)
- Implement #parse and #unparse in GEXF module
